/**
 * Controller for UserManagement screen
 */

app.controller('settingsController', settingsController);

settingsController.$inject = [ '$rootScope', '$scope', '$location',
		'$routeParams','$http', '$cookies', 'defaultService' ];

function settingsController($rootScope, $scope, $location,$routeParams, $http,
		$cookies, defaultService) {

	

}
